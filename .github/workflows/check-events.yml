name: Check Gus Events

on:
  schedule:
    # Run every 30 minutes from 06:00 to 23:30 during sauna season (October-March)
    - cron: "0,30 6-23 * 10-12,1-3 *"
  workflow_dispatch: # Allow manual triggering

jobs:
  check-events:
    runs-on: ubuntu-latest
    permissions:
      contents: write # Allow the action to write to the repository

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "18"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Check for new gus events
        env:
          GMAIL_USER: ${{ secrets.GMAIL_USER }}
          GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}
          NOTIFICATION_EMAIL: ${{ secrets.NOTIFICATION_EMAIL }}
        run: npm run check-events

      - name: Commit updated events file
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add data/last_events.json
          git diff --staged --quiet || git commit -m "Update last events [skip ci]"
          git push
